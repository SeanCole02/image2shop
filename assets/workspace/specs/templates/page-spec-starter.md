# Page Spec Starter

Use `page-intake-template.md` as the canonical starter for new page specs.
Use `../theme-system.md` to anchor cross-page structure.
Use root `THEME_MEMORY.md` to track shared decisions and reusable candidates.
