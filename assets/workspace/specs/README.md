# Specs

This directory stores planning and source-of-truth material used to build the Shopify theme.

Use `theme-system.md` for the shared storefront structure, `pages/` for page-specific specs, and root `THEME_MEMORY.md` for persistent shared memory.
